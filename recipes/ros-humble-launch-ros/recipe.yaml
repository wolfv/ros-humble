# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-humble-launch-ros
  version: 0.19.7
source:
  git: https://github.com/ros2-gbp/launch_ros-release.git
  rev: release/humble/launch_ros/0.19.7-2
  target_directory: ros-humble-launch-ros/src/work

build:
  script: ${{ 'build_ament_python.sh' if unix else 'bld_ament_python.bat' }}
  number: 5
about:
  homepage: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: linux64
      then:
        - sysroot_linux-64 2.17
    - ninja
    - setuptools
    - if: unix
      then:
        - make
        - coreutils
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
    - if: build_platform != target_platform
      then:
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - importlib-metadata
    - numpy
    - pip
    - pytest
    - python
    - pyyaml
    - ros-humble-ament-copyright
    - ros-humble-ament-flake8
    - ros-humble-ament-index-python
    - ros-humble-ament-pep257
    - ros-humble-composition-interfaces
    - ros-humble-launch
    - ros-humble-lifecycle-msgs
    - ros-humble-osrf-pycommon
    - ros-humble-rclpy
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.5 humble
    - setuptools
  run:
    - importlib-metadata
    - python
    - pyyaml
    - ros-humble-ament-index-python
    - ros-humble-composition-interfaces
    - ros-humble-launch
    - ros-humble-lifecycle-msgs
    - ros-humble-osrf-pycommon
    - ros-humble-rclpy
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.5 humble
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
